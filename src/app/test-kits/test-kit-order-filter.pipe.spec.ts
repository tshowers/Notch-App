import { TestKitOrderFilterPipe } from './test-kit-order-filter.pipe';

describe('TestKitOrderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TestKitOrderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
