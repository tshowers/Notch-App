import { KitRegistrationFilterPipe } from './kit-registration-filter.pipe';

describe('KitRegistrationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KitRegistrationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
